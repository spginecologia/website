'use client';

/* * */

import defaultTheme from '@/themes/_default/default.theme';
import { createTheme } from '@mantine/core';

/* * */

export const supportTheme = createTheme({
	//

	...defaultTheme,

	//
});
