# website

# Make sure you have the environment variables file set at apps/frontend

1. Run `docker compose -f compose.local.yml up -d --build --force-recreate --remove-orphans --pull=always` to serve BD

2. `cd apps/frontend`

3. npm i

4. npm run dev
